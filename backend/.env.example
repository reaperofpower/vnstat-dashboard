# VnStat Dashboard Backend Environment Configuration
# Copy this file to .env and configure your values

# API Security
VNSTAT_API_KEY=your-32-character-secure-api-key-here

# Database Configuration
VNSTAT_DB_HOST=localhost
VNSTAT_DB_PORT=3306
VNSTAT_DB_USER=vnstat_user
VNSTAT_DB_PASSWORD=your-secure-database-password
VNSTAT_DB_NAME=vnstat_dashboard

# Server Configuration
VNSTAT_PORT=3000
VNSTAT_HOST=0.0.0.0

# IMPORTANT NOTES:
# 1. Never commit the actual .env file to git
# 2. Use strong, unique passwords for database access
# 3. Generate a secure API key using: openssl rand -base64 32 | tr -d "=+/" | cut -c1-32
# 4. This configuration is only needed if using the legacy index.js
# 5. New installations should use the config.js system instead